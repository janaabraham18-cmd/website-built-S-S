---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import Section from '../components/Section.astro';
import WhyUsSection from '../components/WhyUsSection.astro';
import AdventureCard from '../components/AdventureCard.astro';
import { tours } from '../data/tours';

const teaserSlugs = [
  'etosha-tour',
  'sossusvlei-tour',
  'sandwich-harbour-full-day',
  'quad-bike-tour',
  'dolphin-seal-catamaran-cruise',
  'tandem-skydive',
];
const teaserTours = teaserSlugs
  .map((slug) => tours.find((tour) => tour.slug === slug))
  .filter((tour) => tour !== undefined);

const adventureSlugs = [
  'paragliding',
  'camel-ride',
  'sandboarding',
  'kayaking',
  'living-desert-tour',
  'township-tour',
  'fat-bike-tour',
  'cape-cross',
];
const adventureTours = adventureSlugs
  .map((slug) => tours.find((tour) => tour.slug === slug))
  .filter((tour) => tour !== undefined);

const gallerySlugs = [
  'pelican-point-tour',
  'explorer-tour',
  'special-quad-90min',
  'sandwich-harbour-half-day',
  'fishing',
  'moonlandscape-tour',
  'surf-and-turf',
  'kayak-catamaran-combo',
];
const galleryTours = gallerySlugs
  .map((slug) => tours.find((tour) => tour.slug === slug))
  .filter((tour) => tour !== undefined);

const heroPanels = [
  {
    letter: 'N',
    alt: 'A herd of desert-adapted elephants walking along a dry riverbed in Namibia',
    imageUrl:
      'https://images.unsplash.com/photo-1667389861341-3f1c749cdf39?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'Elina Palm', username: 'drawmeasun' },
  },
  {
    letter: 'A',
    alt: 'A lone oryx crossing the Namib desert plain',
    imageUrl:
      'https://images.unsplash.com/photo-1727880283608-57880fa4edcc?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'Arthur Souquet', username: 'arthursouquet' },
  },
  {
    letter: 'M',
    alt: 'Flamingos wading in the shallow waters of Walvis Bay',
    imageUrl:
      'https://images.unsplash.com/photo-1669805508900-d3eaed6e6c14?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'Vladimir Oprisko', username: 'captoprisko' },
  },
  {
    letter: 'I',
    alt: 'A Cape fur seal resting on the Namibian coast',
    imageUrl:
      'https://images.unsplash.com/photo-1772290226248-156cc5fe4f9b?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'NIR HIMI', username: 'nirhimi' },
  },
  {
    letter: 'B',
    alt: 'A cheetah in the Namibian bush',
    imageUrl:
      'https://images.unsplash.com/photo-1696894132788-d0b1af4c95a7?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'Taylor Wright', username: 'taylorwright22' },
  },
  {
    letter: 'I',
    alt: 'Two zebras on the plains of Etosha National Park',
    imageUrl:
      'https://images.unsplash.com/photo-1579172342894-e6d751ec390c?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'Geranimo', username: 'geraninmo' },
  },
  {
    letter: 'A',
    alt: 'Towering red sand dunes under a clear sky near Sossusvlei',
    imageUrl:
      'https://images.unsplash.com/photo-1613155358201-36bcf3ce695e?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
    imageCredit: { name: 'Mareike Kramper', username: 'mareike_capetown' },
  },
];

const tourCount = tours.filter((tour) => tour.category === 'tour').length;
const adventureCount = tours.filter((tour) => tour.category === 'adventure').length;
const comboCount = tours.filter((tour) => tour.category === 'combo').length;

// Reuses three tours' own already-credited photos rather than sourcing
// new ones, since each is a genuine, strong shot of exactly the
// landscape it's labeling here.
const whereWeGoPanels = [
  { label: 'Desert', copy: 'Placeholder copy: towering red dunes and the salt-white floor of Deadvlei.', slug: 'sossusvlei-tour' },
  { label: 'Coast', copy: "Placeholder copy: shipwrecks, seal colonies, and Namibia's wild Atlantic edge.", slug: 'skeleton-coast-tour' },
  { label: 'Etosha', copy: 'Placeholder copy: elephants, lions, and wide-open pans teeming with wildlife.', slug: 'etosha-tour' },
]
  .map(({ label, copy, slug }) => ({ label, copy, tour: tours.find((t) => t.slug === slug) }))
  .filter((p): p is { label: string; copy: string; tour: NonNullable<typeof p.tour> } => !!p.tour);

const whyUsFeatures = [
  {
    title: 'Based right in Swakopmund',
    description:
      "No call center, no middleman — every tour starts close to home in Swakopmund and Walvis Bay.",
    icon: 'compass' as const,
  },
  {
    title: 'Desert and ocean, one lineup',
    description:
      "From the red dunes of Sossusvlei to the seal colonies of Pelican Point — few operators cover both ends of Namibia's landscape.",
    icon: 'leaf' as const,
  },
  {
    title: `${tourCount + adventureCount + comboCount} ways to mix and match`,
    description: `${tourCount} signature tours, ${adventureCount} adventures, and ${comboCount} combo deals — pair a quad bike ride with a catamaran cruise, or build your own day.`,
    icon: 'users' as const,
  },
  {
    title: 'Guides who know the ground',
    description:
      'Hands-on local guides who know the dunes and the coastline — not a script read from a clipboard.',
    icon: 'shield' as const,
  },
];

// Fresh photography for sections that had none — each sourced separately
// from every other photo already used on the page.
const statsPhoto = {
  imageUrl:
    'https://images.unsplash.com/photo-1637336659714-50c42464957a?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1800',
  alt: 'A dirt road stretching toward distant desert mountains in Namibia',
  credit: { name: 'katsuma tanaka', username: 'kt2080' },
};

const howItWorksPhotos = [
  {
    imageUrl:
      'https://images.unsplash.com/photo-1639402477646-e88e811eef21?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1200',
    alt: 'An empty desert road stretching into the distance in Namibia',
    credit: { name: 'Andrew Svk', username: 'andrew_svk' },
  },
  {
    imageUrl:
      'https://images.unsplash.com/photo-1772289093693-60c4de650e65?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1200',
    alt: 'A safari vehicle driving through the Namibian desert',
    credit: { name: 'NIR HIMI', username: 'nirhimi' },
  },
  {
    imageUrl:
      'https://images.unsplash.com/photo-1592654365893-6f803cdb4913?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1200',
    alt: 'Coastal buildings by the harbor',
    credit: { name: 'Grant Durr', username: 'grant_durr' },
  },
];

const testimonialsPhoto = {
  imageUrl:
    'https://images.unsplash.com/photo-1511988617509-a57c8a288659?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1400',
  alt: 'A group of friends laughing together outdoors',
  credit: { name: 'Helena Lopes', username: 'helenalopesph' },
};

const closingCtaPhoto = {
  imageUrl:
    'https://images.unsplash.com/photo-1783679603167-4d6650f3cbd3?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1800',
  credit: { name: 'GILLES GORLIER', username: 'gorlig_38' },
};
---

<BaseLayout
  title="Home"
  description="Desert and coastal tours from Swakopmund and Walvis Bay, Namibia — Etosha, Sossusvlei, Skeleton Coast, and more with Salt & Sun Tours."
>
  <Hero
    title="Namibia"
    eyebrow="Salt & Sun Tours"
    subhead="Desert and coastal adventures out of Swakopmund and Walvis Bay — from towering red dunes to seal colonies at Pelican Point."
    ctaLabel="Explore our tours"
    ctaHref="/tours"
    panels={heroPanels}
  />

  <div id="scroll-target"></div>

  <section class="stats-band reveal">
    <img
      class="stats-band__bg"
      src={statsPhoto.imageUrl}
      alt={statsPhoto.alt}
      loading="lazy"
      decoding="async"
    />
    <div class="stats-band__overlay" aria-hidden="true" />
    <p class="stats-band__credit">
      Photo:{' '}
      <a
        href={`https://unsplash.com/@${statsPhoto.credit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`}
        target="_blank"
        rel="noopener noreferrer"
      >
        {statsPhoto.credit.name}
      </a>{' '}
      /{' '}
      <a href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Unsplash</a>
    </p>
    <div class="container stats-band__grid">
      <div class="stats-band__stat reveal-item">
        <p class="stats-band__number" data-count-to={tourCount}>{tourCount}</p>
        <p class="stats-band__label">Signature tours</p>
      </div>
      <div class="stats-band__stat reveal-item">
        <p class="stats-band__number" data-count-to={adventureCount}>{adventureCount}</p>
        <p class="stats-band__label">Adventures</p>
      </div>
      <div class="stats-band__stat reveal-item">
        <p class="stats-band__number" data-count-to={comboCount}>{comboCount}</p>
        <p class="stats-band__label">Combo deals</p>
      </div>
      <div class="stats-band__stat reveal-item">
        <p class="stats-band__number" data-count-to={2}>2</p>
        <p class="stats-band__label">Home bases — Swakopmund &amp; Walvis Bay</p>
      </div>
    </div>
  </section>

  <WhyUsSection features={whyUsFeatures} />

  <section class="team-story reveal">
    <div class="container team-story__grid">
      <div class="team-story__photo-wrap reveal-item">
        <img
          class="team-story__photo"
          src="https://images.unsplash.com/photo-1689918000763-73c97d502408?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&q=80&w=1600"
          alt="A guide in the back of a safari vehicle out on the Namibian landscape"
          loading="lazy"
          decoding="async"
        />
        <p class="team-story__credit">
          Photo:{' '}
          <a href="https://unsplash.com/@ultimate_safaris_namibia?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Ultimate Safaris Namibia</a>{' '}
          /{' '}
          <a href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Unsplash</a>
        </p>
      </div>

      <div class="team-story__body reveal-item">
        <p class="team-story__eyebrow">Our story</p>
        <h2>A small team, based right here</h2>
        <p>
          Placeholder copy — swap in the real story here. Salt &amp; Sun
          Tours is run by a small, Swakopmund-based team who grew up on
          this coastline and still lead most tours personally. We're
          hands-on from your first message through to the drop-off at
          the end of the day.
        </p>
        <a class="btn btn-outline" href="/contact">Get in touch</a>
      </div>
    </div>
  </section>

  <section class="how-it-works reveal">
    <div class="container">
      <div class="how-it-works__intro reveal-item">
        <p class="how-it-works__eyebrow">How it works</p>
        <h2>Three simple steps</h2>
      </div>

      <ol class="how-it-works__grid">
        <li class="how-it-works__item reveal-item">
          <div class="how-it-works__photo-wrap">
            <img
              class="how-it-works__photo"
              src={howItWorksPhotos[0].imageUrl}
              alt={howItWorksPhotos[0].alt}
              loading="lazy"
              decoding="async"
            />
            <p class="how-it-works__credit">
              Photo:{' '}
              <a href={`https://unsplash.com/@${howItWorksPhotos[0].credit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`} target="_blank" rel="noopener noreferrer">{howItWorksPhotos[0].credit.name}</a>{' '}
              /{' '}
              <a href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Unsplash</a>
            </p>
          </div>
          <span class="how-it-works__step" aria-hidden="true">01</span>
          <h3>Tell us your dates</h3>
          <p>
            Placeholder copy. Send us your travel dates and what you're
            hoping to see, whether that's Etosha's wildlife or the
            dunes at Sossusvlei.
          </p>
        </li>
        <li class="how-it-works__item reveal-item">
          <div class="how-it-works__photo-wrap">
            <img
              class="how-it-works__photo"
              src={howItWorksPhotos[1].imageUrl}
              alt={howItWorksPhotos[1].alt}
              loading="lazy"
              decoding="async"
            />
            <p class="how-it-works__credit">
              Photo:{' '}
              <a href={`https://unsplash.com/@${howItWorksPhotos[1].credit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`} target="_blank" rel="noopener noreferrer">{howItWorksPhotos[1].credit.name}</a>{' '}
              /{' '}
              <a href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Unsplash</a>
            </p>
          </div>
          <span class="how-it-works__step" aria-hidden="true">02</span>
          <h3>We build your day around you</h3>
          <p>
            Placeholder copy. We put together a plan that fits your
            dates, your pace, and the people you're traveling with —
            no fixed package required.
          </p>
        </li>
        <li class="how-it-works__item reveal-item">
          <div class="how-it-works__photo-wrap">
            <img
              class="how-it-works__photo"
              src={howItWorksPhotos[2].imageUrl}
              alt={howItWorksPhotos[2].alt}
              loading="lazy"
              decoding="async"
            />
            <p class="how-it-works__credit">
              Photo:{' '}
              <a href={`https://unsplash.com/@${howItWorksPhotos[2].credit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`} target="_blank" rel="noopener noreferrer">{howItWorksPhotos[2].credit.name}</a>{' '}
              /{' '}
              <a href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Unsplash</a>
            </p>
          </div>
          <span class="how-it-works__step" aria-hidden="true">03</span>
          <h3>Meet us in Swakopmund</h3>
          <p>
            Placeholder copy. Meet your guide in Swakopmund or Walvis
            Bay and head out — every tour starts and ends close to
            home.
          </p>
        </li>
      </ol>
    </div>
  </section>

  <section class="tours-teaser reveal">
    <div class="container tours-teaser__intro reveal-item">
      <p class="tours-teaser__eyebrow">Our offering</p>
      <h2>A few of our tours</h2>
      <p class="tours-teaser__body">
        A sample of what we run out of Swakopmund and Walvis Bay — see
        the full lineup, including every adventure and combo deal, on
        the tours page.
      </p>
    </div>

    <div class="tours-teaser__pin">
      <div class="tours-teaser__track">
        {
          teaserTours.map((tour) => (
            <div class="tours-teaser__item">
              <AdventureCard tour={tour} />
            </div>
          ))
        }
      </div>
    </div>

    <div class="container tours-teaser__cta reveal-item">
      <a class="btn btn-outline" href="/tours">View all tours</a>
    </div>
  </section>

  <section class="where-we-go reveal">
    <div class="container where-we-go__intro reveal-item">
      <p class="where-we-go__eyebrow">Where we go</p>
      <h2>Three landscapes, one coastline</h2>
    </div>

    <div class="container where-we-go__grid">
      {
        whereWeGoPanels.map(({ label, copy, tour }) => (
          <figure class="where-we-go__panel">
            {tour.imageUrl && <img src={tour.imageUrl} alt={tour.name} loading="lazy" decoding="async" />}
            <div class="where-we-go__scrim" aria-hidden="true" />
            <figcaption class="where-we-go__text">
              <p class="where-we-go__label">{label}</p>
              <p class="where-we-go__copy">{copy}</p>
            </figcaption>
            {tour.imageCredit && (
              <p class="where-we-go__credit">
                Photo:{' '}
                <a
                  href={`https://unsplash.com/@${tour.imageCredit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {tour.imageCredit.name}
                </a>{' '}
                /{' '}
                <a
                  href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral"
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  Unsplash
                </a>
              </p>
            )}
          </figure>
        ))
      }
    </div>
  </section>

  <section class="homepage-adventures reveal">
    <div class="container homepage-adventures__intro reveal-item">
      <p class="homepage-adventures__eyebrow">Beyond the tours</p>
      <h2>More ways to spend a day</h2>
      <p class="homepage-adventures__body">
        Shorter, activity-based excursions around Swakopmund and Walvis
        Bay — the full lineup, including every combo deal, is on the
        tours page.
      </p>
    </div>

    <div class="container homepage-adventures__grid">
      {adventureTours.map((tour, i) => <AdventureCard tour={tour} featured={i === 0} />)}
    </div>

    <div class="container homepage-adventures__cta reveal-item">
      <a class="btn btn-outline" href="/tours#adventure">See every adventure</a>
    </div>
  </section>

  <section class="gallery reveal">
    <div class="container gallery__intro reveal-item">
      <p class="gallery__eyebrow">A few more moments</p>
      <h2>Namibia, in passing</h2>
    </div>

    <div class="container gallery__grid">
      {
        galleryTours.map((tour) => (
          <figure class="gallery__item reveal-item">
            {tour.imageUrl && <img src={tour.imageUrl} alt={tour.name} loading="lazy" decoding="async" />}
            {tour.imageCredit && (
              <figcaption class="gallery__credit">
                Photo:{' '}
                <a
                  href={`https://unsplash.com/@${tour.imageCredit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {tour.imageCredit.name}
                </a>{' '}
                /{' '}
                <a
                  href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral"
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  Unsplash
                </a>
              </figcaption>
            )}
          </figure>
        ))
      }
    </div>
  </section>

  <section class="testimonials reveal">
    <div class="container testimonials__intro reveal-item">
      <p class="testimonials__eyebrow">What guests say</p>
      <h2>Placeholder — real reviews coming soon</h2>
    </div>

    <div class="container testimonials__photo-wrap reveal-item">
      <img
        class="testimonials__photo"
        src={testimonialsPhoto.imageUrl}
        alt={testimonialsPhoto.alt}
        loading="lazy"
        decoding="async"
      />
      <p class="testimonials__photo-credit">
        Photo:{' '}
        <a
          href={`https://unsplash.com/@${testimonialsPhoto.credit.username}?utm_source=salt-and-sun-tours&utm_medium=referral`}
          target="_blank"
          rel="noopener noreferrer"
        >
          {testimonialsPhoto.credit.name}
        </a>{' '}
        /{' '}
        <a href="https://unsplash.com/?utm_source=salt-and-sun-tours&utm_medium=referral" target="_blank" rel="noopener noreferrer">Unsplash</a>
      </p>
    </div>

    <ul class="testimonials__grid">
      <li class="testimonials__item reveal-item">
        <p class="testimonials__quote">
          "Placeholder quote — swap in a real review. Planning was easy
          and our guide clearly knew the desert inside out."
        </p>
        <p class="testimonials__name">Placeholder Name</p>
        <p class="testimonials__trip">Sossusvlei Tour</p>
      </li>
      <li class="testimonials__item reveal-item">
        <p class="testimonials__quote">
          "Placeholder quote — swap in a real review. We saw more
          wildlife in one day than we expected for the whole trip."
        </p>
        <p class="testimonials__name">Placeholder Name</p>
        <p class="testimonials__trip">Etosha Tour</p>
      </li>
      <li class="testimonials__item reveal-item">
        <p class="testimonials__quote">
          "Placeholder quote — swap in a real review. Dolphins right
          next to the boat, and the crew made the whole morning fun."
        </p>
        <p class="testimonials__name">Placeholder Name</p>
        <p class="testimonials__trip">Dolphin &amp; Seal Catamaran Cruise</p>
      </li>
      <li class="testimonials__item reveal-item">
        <p class="testimonials__quote">
          "Placeholder quote — swap in a real review. Fast, friendly,
          and no hard sell — just a great day on the dunes."
        </p>
        <p class="testimonials__name">Placeholder Name</p>
        <p class="testimonials__trip">Quad Bike Tour</p>
      </li>
    </ul>
  </section>

  <section class="trust-band reveal">
    <div class="container trust-band__inner">
      <div class="trust-band__intro reveal-item">
        <p class="trust-band__eyebrow">Why travelers choose us</p>
        <h2>Straight answers from people who know the ground</h2>
      </div>

      <ul class="trust-band__grid">
        <li class="trust-band__item reveal-item">
          <h3>Local, not outsourced</h3>
          <p>
            Every tour starts and ends in Swakopmund or Walvis Bay, run by
            the same small team from your first message to the final
            drop-off.
          </p>
        </li>
        <li class="trust-band__item reveal-item">
          <h3>Built for how you want to travel</h3>
          <p>
            {tourCount + adventureCount + comboCount} tours, adventures, and combo deals — from a
            30-minute quad ride to a full day in Etosha, mixed and matched
            to your trip.
          </p>
        </li>
        <li class="trust-band__item reveal-item">
          <h3>A straight answer, fast</h3>
          <p>
            No call center. Reach out and you'll hear back from someone
            who actually knows the dunes and the coastline.
          </p>
        </li>
      </ul>
    </div>
  </section>

  <section class="faq-preview reveal">
    <div class="container faq-preview__intro reveal-item">
      <p class="faq-preview__eyebrow">Questions</p>
      <h2>A few things people ask</h2>
    </div>

    <div class="container faq-preview__grid">
      <div class="faq-preview__item reveal-item">
        <h3>Do I need to book in advance?</h3>
        <p>
          Placeholder answer — most tours can be arranged with a few
          days' notice, but popular dates fill up faster in peak
          season.
        </p>
      </div>
      <div class="faq-preview__item reveal-item">
        <h3>Where do tours depart from?</h3>
        <p>
          Placeholder answer — most tours depart from Swakopmund or
          Walvis Bay; we'll confirm your pick-up point when we plan
          your day.
        </p>
      </div>
      <div class="faq-preview__item reveal-item">
        <h3>Can tours be combined?</h3>
        <p>
          Placeholder answer — yes, many guests pair a tour with a
          shorter adventure or a combo deal; see the tours page for
          options.
        </p>
      </div>
      <div class="faq-preview__item reveal-item">
        <h3>What should I bring?</h3>
        <p>
          Placeholder answer — comfortable shoes, sun protection, and
          a camera; we'll flag anything tour-specific when we confirm
          your booking.
        </p>
      </div>
    </div>

    <p class="faq-preview__more reveal-item">
      Have a different question? <a href="/contact">Get in touch</a>.
    </p>
  </section>

  <Section
    eyebrow="Ready when you are"
    heading="Plan your journey"
    align="center"
    theme="photo"
    imageUrl={closingCtaPhoto.imageUrl}
    imageCredit={closingCtaPhoto.credit}
  >
    <p>
      Ready to see the dunes, the coastline, and everything in between?
      Get in touch and we'll help you build the right day — or days —
      around Swakopmund and Walvis Bay.
    </p>
    <a class="btn btn-primary" href="/booking">Start booking</a>
  </Section>
</BaseLayout>

<style>
  .tours-teaser {
    /* Generous top space specifically: the section right above this one
       (Skeleton Coast) is itself a pinned/scrubbed scroll effect, and the
       cards below are a second one (see setupToursTeaserSlide()). Without
       real breathing room here the two scroll-jacked moments run straight
       into each other with no normal scrolling in between. */
    padding-block: clamp(6rem, 14vw, 11rem) var(--space-2xl);
  }

  .tours-teaser__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-2xl);
  }

  .tours-teaser__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .tours-teaser__body {
    color: var(--color-text-muted);
    margin: 0;
  }

  /* Edge-to-edge horizontal slider. Base state is a plain scrollable row
     (works with no JS, under reduced motion, and on touch/mobile); the
     motion controller upgrades it to a scroll-pinned slide on desktop
     when the visitor hasn't asked for reduced motion — see
     setupToursTeaserSlide() in scripts/motion.ts. */
  .tours-teaser__pin {
    position: relative;
    width: 100%;
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }

  .tours-teaser__pin::-webkit-scrollbar {
    display: none;
  }

  .tours-teaser__pin--pinned {
    overflow: hidden;
  }

  .tours-teaser__track {
    display: flex;
    gap: var(--space-lg);
    width: max-content;
    padding-inline: clamp(1.25rem, 4vw, 2.5rem);
    scroll-snap-type: x proximity;
    will-change: transform;
  }

  .tours-teaser__pin--pinned .tours-teaser__track {
    scroll-snap-type: none;
  }

  .tours-teaser__item {
    flex: 0 0 min(78vw, 380px);
    height: 420px;
    scroll-snap-align: start;
  }

  .tours-teaser__item :global(.adventure-card) {
    width: 100%;
    height: 100%;
  }

  .tours-teaser__cta {
    text-align: center;
    margin-top: var(--space-xl);
  }

  @media (max-width: 640px) {
    .tours-teaser__item {
      height: 340px;
    }
  }

  /* ---------- Trust band (replaces fabricated testimonials) ---------- */
  .trust-band {
    padding-block: var(--space-2xl);
  }

  .trust-band__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .trust-band__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .trust-band__intro h2 {
    margin: 0;
  }

  .trust-band__grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    max-width: var(--content-width);
    margin-inline: auto;
  }

  .trust-band__item {
    text-align: center;
    padding: var(--space-lg);
    border-top: 2px solid var(--color-accent-soft);
  }

  .trust-band__item h3 {
    font-size: var(--fs-lg);
    margin: 0 0 var(--space-2xs);
  }

  .trust-band__item p {
    font-size: var(--fs-base);
    line-height: var(--lh-normal);
    color: var(--color-text-muted);
    margin: 0;
  }

  @media (max-width: 860px) {
    .trust-band__grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }
  }

  /* ---------- Stats band ---------- */
  .stats-band {
    position: relative;
    padding-block: var(--space-2xl);
    overflow: hidden;
    color: var(--color-text-on-dark);
  }

  .stats-band__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .stats-band__overlay {
    position: absolute;
    inset: 0;
    background: var(--gradient-overlay-photo-strong);
  }

  .stats-band__credit {
    position: absolute;
    right: var(--space-sm);
    bottom: var(--space-sm);
    z-index: 2;
    margin: 0;
    font-size: var(--fs-xs);
    color: rgba(251, 247, 241, 0.65);
  }

  .stats-band__credit a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .stats-band__grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
    text-align: center;
  }

  .stats-band__number {
    font-family: var(--font-display);
    font-size: var(--fs-2xl);
    color: var(--color-text-on-dark);
    margin: 0;
  }

  .stats-band__label {
    font-size: var(--fs-sm);
    color: var(--color-sand-100);
    margin: var(--space-3xs) 0 0;
  }

  @media (max-width: 760px) {
    .stats-band__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-lg) var(--space-md);
    }
  }

  /* ---------- Homepage adventures grid ---------- */
  .homepage-adventures {
    padding-block: var(--space-2xl);
  }

  .homepage-adventures__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .homepage-adventures__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .homepage-adventures__body {
    color: var(--color-text-muted);
    margin: 0;
  }

  .homepage-adventures__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-auto-rows: 240px;
    grid-auto-flow: dense;
    gap: var(--space-md);
  }

  /* Hover tilt — see setupAdventureTilt() in scripts/motion.ts, which
     drives the rotate/scale transform itself; this just handles the
     shadow lift and keeps the tilted card above its neighbors. */
  .homepage-adventures__grid :global(.adventure-card) {
    transition: box-shadow var(--duration-base) var(--ease-out);
  }

  .homepage-adventures__grid :global(.adventure-card.is-tilting) {
    box-shadow: var(--shadow-lg);
    z-index: 1;
  }

  .homepage-adventures__cta {
    text-align: center;
    margin-top: var(--space-xl);
  }

  @media (max-width: 960px) {
    .homepage-adventures__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .homepage-adventures__grid {
      grid-template-columns: 1fr;
    }
  }

  /* ---------- Gallery ---------- */
  .gallery {
    padding-block: var(--space-2xl);
  }

  .gallery__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .gallery__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  /* True masonry via CSS columns instead of a fixed-height grid — each
     photo keeps its natural aspect ratio (Unsplash serves these at a set
     width with height left to scale, so ratios genuinely vary) rather
     than being cropped into identical boxes. */
  .gallery__grid {
    columns: 4;
    column-gap: var(--space-sm);
  }

  .gallery__item {
    position: relative;
    margin: 0 0 var(--space-sm);
    overflow: hidden;
    border-radius: var(--radius-md);
    break-inside: avoid;
  }

  .gallery__item img {
    display: block;
    width: 100%;
    height: auto;
    transition: transform var(--duration-slow) var(--ease-out);
  }

  .gallery__item:hover img {
    transform: scale(1.06);
  }

  .gallery__credit {
    position: absolute;
    right: var(--space-2xs);
    bottom: var(--space-2xs);
    z-index: 1;
    margin: 0;
    font-size: 0.65rem;
    color: rgba(251, 247, 241, 0.75);
  }

  .gallery__credit a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  @media (max-width: 960px) {
    .gallery__grid {
      columns: 2;
    }
  }

  @media (max-width: 640px) {
    .gallery__grid {
      columns: 1;
    }
  }

  /* ---------- Meet the team / our story ---------- */
  .team-story {
    padding-block: var(--space-2xl);
  }

  .team-story__grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    align-items: center;
    gap: var(--space-2xl);
  }

  .team-story__photo-wrap {
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
  }

  .team-story__photo {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    display: block;
    will-change: transform;
  }

  .team-story__credit {
    position: absolute;
    right: var(--space-sm);
    bottom: var(--space-sm);
    margin: 0;
    font-size: var(--fs-xs);
    color: rgba(251, 247, 241, 0.85);
  }

  .team-story__credit a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .team-story__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .team-story__body h2 {
    margin: 0 0 var(--space-md);
  }

  .team-story__body p {
    color: var(--color-text-muted);
    line-height: var(--lh-normal);
    margin: 0 0 var(--space-lg);
  }

  @media (max-width: 860px) {
    .team-story__grid {
      grid-template-columns: 1fr;
    }

    .team-story__photo {
      aspect-ratio: 4 / 3;
    }
  }

  /* ---------- How it works ---------- */
  .how-it-works {
    padding-block: var(--space-2xl);
  }

  .how-it-works__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .how-it-works__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .how-it-works__intro h2 {
    margin: 0;
  }

  .how-it-works__grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl) var(--space-lg);
    max-width: var(--content-width);
    margin-inline: auto;
  }

  .how-it-works__item {
    text-align: center;
    padding: var(--space-lg);
  }

  .how-it-works__photo-wrap {
    position: relative;
    aspect-ratio: 4 / 3;
    margin-bottom: var(--space-sm);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .how-it-works__photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .how-it-works__credit {
    position: absolute;
    right: var(--space-2xs);
    bottom: var(--space-2xs);
    z-index: 1;
    margin: 0;
    font-size: 0.65rem;
    color: rgba(251, 247, 241, 0.75);
  }

  .how-it-works__credit a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .how-it-works__step {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 3rem;
    height: 3rem;
    margin-bottom: var(--space-sm);
    border-radius: 50%;
    background: var(--color-sand-100);
    color: var(--color-accent-strong);
    font-family: var(--font-display);
    font-weight: 600;
    font-size: var(--fs-base);
  }

  .how-it-works__item h3 {
    font-size: var(--fs-lg);
    margin: 0 0 var(--space-2xs);
  }

  .how-it-works__item p {
    font-size: var(--fs-base);
    line-height: var(--lh-normal);
    color: var(--color-text-muted);
    margin: 0;
  }

  @media (max-width: 860px) {
    .how-it-works__grid {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
  }

  /* ---------- Where we go ---------- */
  .where-we-go {
    padding-block: var(--space-2xl);
  }

  .where-we-go__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .where-we-go__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .where-we-go__intro h2 {
    margin: 0;
  }

  .where-we-go__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
  }

  .where-we-go__panel {
    position: relative;
    margin: 0;
    aspect-ratio: 3 / 4;
    overflow: hidden;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
  }

  .where-we-go__panel img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--duration-slow) var(--ease-out);
  }

  .where-we-go__panel:hover img {
    transform: scale(1.06);
  }

  .where-we-go__scrim {
    position: absolute;
    inset: 0;
    background: var(--gradient-overlay-photo-strong);
  }

  .where-we-go__text {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1;
    padding: var(--space-md);
  }

  .where-we-go__label {
    font-family: var(--font-display-italic);
    font-style: italic;
    font-weight: 600;
    font-size: var(--fs-xl);
    color: var(--color-text-on-dark);
    margin: 0 0 var(--space-3xs);
  }

  .where-we-go__copy {
    font-size: var(--fs-sm);
    line-height: var(--lh-normal);
    color: var(--color-sand-100);
    margin: 0;
  }

  .where-we-go__credit {
    position: absolute;
    right: var(--space-2xs);
    top: var(--space-2xs);
    z-index: 1;
    margin: 0;
    font-size: 0.65rem;
    color: rgba(251, 247, 241, 0.75);
  }

  .where-we-go__credit a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  @media (max-width: 860px) {
    .where-we-go__grid {
      grid-template-columns: 1fr;
    }

    .where-we-go__panel {
      aspect-ratio: 4 / 3;
    }
  }

  /* ---------- Testimonials ---------- */
  .testimonials {
    padding-block: var(--space-2xl);
  }

  .testimonials__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .testimonials__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .testimonials__intro h2 {
    margin: 0;
  }

  .testimonials__photo-wrap {
    position: relative;
    max-width: var(--content-width);
    aspect-ratio: 21 / 6;
    margin: 0 auto var(--space-xl);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-md);
  }

  .testimonials__photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .testimonials__photo-credit {
    position: absolute;
    right: var(--space-sm);
    bottom: var(--space-sm);
    z-index: 1;
    margin: 0;
    font-size: var(--fs-xs);
    color: rgba(251, 247, 241, 0.75);
  }

  .testimonials__photo-credit a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  @media (max-width: 640px) {
    .testimonials__photo-wrap {
      aspect-ratio: 4 / 3;
    }
  }

  .testimonials__grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
    max-width: var(--content-width);
    margin-inline: auto;
  }

  .testimonials__item {
    display: flex;
    flex-direction: column;
    padding: var(--space-lg);
    background: var(--color-bg-alt);
    border-radius: var(--radius-md);
  }

  .testimonials__quote {
    flex: 1;
    font-family: var(--font-display-italic);
    font-style: italic;
    font-size: var(--fs-base);
    line-height: var(--lh-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .testimonials__name {
    font-weight: 600;
    font-size: var(--fs-sm);
    color: var(--color-earth-900);
    margin: 0;
  }

  .testimonials__trip {
    font-size: var(--fs-xs);
    color: var(--color-accent-strong);
    margin: var(--space-3xs) 0 0;
  }

  @media (max-width: 960px) {
    .testimonials__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 640px) {
    .testimonials__grid {
      grid-template-columns: 1fr;
    }
  }

  /* ---------- FAQ preview ---------- */
  .faq-preview {
    padding-block: var(--space-2xl);
  }

  .faq-preview__intro {
    text-align: center;
    max-width: var(--content-width-narrow);
    margin-inline: auto;
    margin-bottom: var(--space-xl);
  }

  .faq-preview__eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-accent-strong);
    margin-bottom: var(--space-2xs);
  }

  .faq-preview__intro h2 {
    margin: 0;
  }

  .faq-preview__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg) var(--space-xl);
    max-width: var(--content-width);
    margin-inline: auto;
  }

  .faq-preview__item h3 {
    font-size: var(--fs-lg);
    margin: 0 0 var(--space-2xs);
  }

  .faq-preview__item p {
    font-size: var(--fs-base);
    line-height: var(--lh-normal);
    color: var(--color-text-muted);
    margin: 0;
  }

  .faq-preview__more {
    text-align: center;
    color: var(--color-text-muted);
    margin: var(--space-xl) 0 0;
  }

  .faq-preview__more a {
    color: var(--color-accent-strong);
    font-weight: 600;
  }

  @media (max-width: 640px) {
    .faq-preview__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
